## cert dump

Fetch and display certificates for a host or PEM file(s)

### Synopsis

Fetch and display certificates for one or more targets.

Targets may be:
- A TLS endpoint in host:port form (e.g., example.com:443) — the peer chain is
  retrieved and displayed.
- A path to a PEM/DER certificate file — the certificate is displayed.

Use --leaf-only to print only the leaf certificate when connecting to a host.

```
cert dump [host:port|cert.pem]... [flags]
```

### Options

```
  -h, --help          help for dump
  -l, --leaf-only     only display the leaf certificate
  -s, --show-hashes   show hashes of all certificates in the chain
```

### Options inherited from parent commands

```
      --ca string                   CA certificate bundle file
      --config string               config file (default is $HOME/.config/goutils/cert.yaml)
      --debug                       enable debug mode
  -d, --display-mode string         hex display mode for SKI (default "lower")
  -i, --intermediates-file string   intermediate certificate bundle
  -q, --quiet                       enable quiet mode
  -k, --skip-verify                 skip certificate verification
      --strict-tls                  use strict TLS settings
  -v, --verbose                     verbose output
```

### SEE ALSO

* [cert](cert.md)	 - A certificate swiss‑army knife

###### Auto generated by spf13/cobra on 24-Nov-2025
