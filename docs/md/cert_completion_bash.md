## cert completion bash

Generate the autocompletion script for bash

### Synopsis

Generate the autocompletion script for the bash shell.

This script depends on the 'bash-completion' package.
If it is not installed already, you can install it via your OS's package manager.

To load completions in your current shell session:

	source <(cert completion bash)

To load completions for every new session, execute once:

#### Linux:

	cert completion bash > /etc/bash_completion.d/cert

#### macOS:

	cert completion bash > $(brew --prefix)/etc/bash_completion.d/cert

You will need to start a new shell for this setup to take effect.


```
cert completion bash
```

### Options

```
  -h, --help              help for bash
      --no-descriptions   disable completion descriptions
```

### Options inherited from parent commands

```
      --ca string                   CA certificate bundle file
      --config string               config file (default is $HOME/.config/goutils/cert.yaml)
      --debug                       enable debug mode
  -d, --display-mode string         hex display mode for SKI (default "lower")
  -i, --intermediates-file string   intermediate certificate bundle
  -q, --quiet                       enable quiet mode
  -k, --skip-verify                 skip certificate verification
      --strict-tls                  use strict TLS settings
  -v, --verbose                     verbose output
```

### SEE ALSO

* [cert completion](cert_completion.md)	 - Generate the autocompletion script for the specified shell

###### Auto generated by spf13/cobra on 21-Nov-2025
