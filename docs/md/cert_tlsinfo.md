## cert tlsinfo

Connect and print TLS connection details

### Synopsis

Connects to one or more TLS endpoints (proxy‑aware dialer) and prints
TLS version, cipher suite, and peer certificate subjects/issuers. Note: the
connection is made with InsecureSkipVerify=true to retrieve details; this does
not validate the peer.

```
cert tlsinfo <host:port> [more ...] [flags]
```

### Options

```
  -h, --help              help for tlsinfo
  -s, --sni-name string   SNI name to use when connecting
```

### Options inherited from parent commands

```
      --ca string                   CA certificate bundle file
      --config string               config file (default is $HOME/.config/goutils/cert.yaml)
      --debug                       enable debug mode
  -d, --display-mode string         hex display mode for SKI (default "lower")
  -i, --intermediates-file string   intermediate certificate bundle
  -q, --quiet                       enable quiet mode
  -k, --skip-verify                 skip certificate verification
      --strict-tls                  use strict TLS settings
  -v, --verbose                     verbose output
```

### SEE ALSO

* [cert](cert.md)	 - A certificate swiss‑army knife

###### Auto generated by spf13/cobra on 25-Nov-2025
